import 'package:flutter/widgets.dart';

/// Затемнение фона под диалогами — дефолт shadcn_ui (`0xcc000000`, ~80%
/// чёрного) выглядит как полностью чёрный экран поверх и так тёмной темы
/// приложения. Первая попытка приглушить это (`0x14000000`, ~8%) ушла в
/// другую крайность — на тёмном фоне такое затемнение неотличимо от его
/// отсутствия, барьер визуально не читался вообще. Держим заметное, но не
/// сплошное затемнение: фон (глобус, список серверов) остаётся угадываемым
/// под диалогом, а сам диалог явно выделяется поверх него.
const dialogBarrierColor = Color(0x66000000);
