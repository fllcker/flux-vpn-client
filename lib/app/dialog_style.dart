import 'package:flutter/widgets.dart';

/// Затемнение фона под диалогами — дефолт shadcn_ui (`0xcc000000`, ~80%
/// чёрного) выглядит как полностью чёрный экран, а поскольку у приложения и
/// так тёмная тема (фон почти чёрный), даже 20% чёрного поверх него уже
/// неотличимо от сплошной заливки — фон и так тёмный, добавлять почти
/// нечего. Держим совсем небольшое затемнение, чтобы фон (глобус, список
/// серверов) оставался ясно виден под диалогом, а разделение с карточкой
/// диалога держится на её собственной рамке/тени.
const dialogBarrierColor = Color(0x14000000);
